# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
slowapi>=0.1.9

# LLM
anthropic>=0.39.0

# Storage (Tigris / S3-compatible). Provisioned via `fly storage create`.
boto3>=1.34.0

# Data log parsing (Excel → markdown table)
openpyxl>=3.1.0
pandas>=2.0.0

# Option A: Local model
# vllm>=0.4.0
# or
# llama-cpp-python>=0.2.0

# Analysis engine
numpy>=1.24.0
xarray>=2023.1.0
matplotlib>=3.7.0
scipy>=1.10.0
# Kaindl .pxt loader — cherry-picked from pyarpes (arpes/load_pxt.py).
# igor2 is the maintained successor to chstan's igor fork, supports Python 3.6+.
igor2>=0.5.0
Pillow>=10.0.0

# Knowledge / RAG
# chromadb>=0.4.0  # uncomment when RAG is implemented
# sentence-transformers>=2.2.0  # for embeddings

# Auth
# TODO: add auth dependencies based on chosen method
# python-jose[cryptography]  # for JWT
# authlib  # for OAuth

# Tests
pytest>=8.0.0
httpx>=0.27.0  # needed by fastapi.testclient

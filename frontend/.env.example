# Frontend environment variables (Vite). Copy to `.env` for local dev,
# and set the same key in Cloudflare Pages → Settings → Environment variables
# for production builds.

# Backend base URL. Defaults to http://localhost:8000 if unset.
VITE_API_URL=http://localhost:8000
